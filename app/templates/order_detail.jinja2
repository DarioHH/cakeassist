{% extends "layout.html" %}
{% load crispy_forms_tags %}

{% block body%}
<div class="card">

        <div class="card-header">

            Order
        </div>
        <div class="card-body">
        {{ order }}
        </div>
        {% for item in  items %}
          {{ item.cake}} {{ item.quantity}}
        {% endfor %}
    </div>
</table>
{% endblock %}

